# Autonomous R&D Intelligence Layer - Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git - it contains secrets!

# ============================================================================
# REQUIRED: Google Cloud Configuration
# ============================================================================
PROJECT_ID=periodicdent42
LOCATION=us-central1
ENVIRONMENT=development

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# Authentication (set to true in production!)
ENABLE_AUTH=false

# API Key - NEVER hardcode! Get from Secret Manager or set temporarily for local dev
# For local dev: export API_KEY="your-test-key"
# For production: Loaded automatically from Secret Manager (secret: api-key)
API_KEY=

# CORS Origins - comma-separated list of allowed origins
# Development: leave empty (defaults to localhost)
# Production: set to your frontend domain(s)
ALLOWED_ORIGINS=

# Rate Limiting - requests per IP per minute
RATE_LIMIT_PER_MINUTE=60

# ============================================================================
# OPTIONAL: Database Configuration (if using Cloud SQL)
# ============================================================================

# Cloud SQL instance connection name (format: project:region:instance)
GCP_SQL_INSTANCE=

# Database credentials - get from Secret Manager in production
DB_USER=ard_user
DB_PASSWORD=
DB_NAME=ard_intelligence
DB_HOST=localhost
DB_PORT=5432

# ============================================================================
# OPTIONAL: Cloud Storage
# ============================================================================

# GCS bucket name for storing experiment results
GCS_BUCKET=

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

PORT=8080
LOG_LEVEL=INFO

# ============================================================================
# FEATURE FLAGS
# ============================================================================

ENABLE_METRICS=true
ENABLE_TRACING=true

# ============================================================================
# HOW TO GET SECRETS FROM SECRET MANAGER
# ============================================================================
#
# For local development:
# 1. Authenticate: gcloud auth application-default login
# 2. Get API key: gcloud secrets versions access latest --secret=api-key --project=periodicdent42
# 3. Set in terminal: export API_KEY="<value-from-above>"
#
# For production (Cloud Run):
# Secrets are automatically loaded from Secret Manager via --set-secrets flag
# See: infra/scripts/deploy_cloudrun.sh
#
# ============================================================================

