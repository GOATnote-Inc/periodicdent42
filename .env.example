# ========================================
# Autonomous R&D Intelligence Layer
# Environment Configuration Template
# ========================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace placeholder values with real credentials
# 3. NEVER commit .env to version control
#
# SECURITY NOTICE:
# - Production: Use Google Cloud Secret Manager (secrets are auto-loaded)
# - Local dev: Set environment variables or use .env
# - Rotate credentials every 90 days
# - Report suspicious activity to b@thegoatnote.com
#

# ========================================
# Google Cloud Platform
# ========================================
PROJECT_ID=periodicdent42
GCP_REGION=us-central1

# Cloud Run (production only)
# SERVICE_URL=https://ard-backend-dydzexswua-uc.a.run.app

# ========================================
# Database (Cloud SQL PostgreSQL 15)
# ========================================
# Production: Uses Unix socket via Cloud SQL Proxy
# Local: Connect via Cloud SQL Proxy on localhost:5433
DB_USER=ard_user
DB_PASSWORD=<SECRET_REPLACE_ME>  # DO NOT COMMIT REAL PASSWORD
DB_NAME=ard_intelligence
DB_HOST=localhost  # Cloud SQL Proxy
DB_PORT=5433

# Production connection (uncomment for Cloud Run)
# DB_HOST=/cloudsql/periodicdent42:us-central1:ard-intelligence-db
# DB_PORT=5432

# ========================================
# Vertex AI (Gemini 2.5 Flash + Pro)
# ========================================
# Credentials: Use Application Default Credentials (ADC)
# gcloud auth application-default login

# Model configuration
FLASH_MODEL=gemini-2.0-flash-001
PRO_MODEL=gemini-2.0-pro-001

# Timeout configuration (seconds)
FLASH_TIMEOUT_S=5.0
PRO_TIMEOUT_S=45.0

# ========================================
# Security
# ========================================
# API Key for write operations (optional, recommended for production)
# API_KEY=<GENERATE_RANDOM_32_CHAR_KEY>

# CORS origins (comma-separated, no spaces)
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000,https://ard-backend-dydzexswua-uc.a.run.app

# ========================================
# Logging
# ========================================
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL

# ========================================
# CI/CD (GitHub Actions only)
# ========================================
# GITHUB_SHA=<commit_sha>
# GITHUB_REF_NAME=<branch_name>
# CI_DURATION_SEC=<duration>
# CI_STATUS=<success|failure>

# ========================================
# DVC (Data Version Control)
# ========================================
# DVC_REMOTE=gs://periodicdent42-data
# DVC_CACHE_DIR=.dvc/cache

# ========================================
# Experiment Tracking
# ========================================
# EXPERIMENT_BUCKET=gs://periodicdent42-experiments
# TELEMETRY_BUCKET=gs://periodicdent42-telemetry

# ========================================
# Performance & Cost Controls
# ========================================
# Max cost per CI run (USD)
MAX_CI_COST_USD=1.00

# Max walltime per CI run (seconds)
MAX_CI_WALLTIME_SEC=1800

# Epistemic CI budget (50% of full suite by default)
CI_BUDGET_FRACTION=0.5

# ========================================
# Development & Testing
# ========================================
# Enable mock mode for local testing (no real API calls)
# MOCK_MODE=false

# Pytest random seed (for reproducibility)
# PYTEST_RANDOM_SEED=42

# ========================================
# Secrets Rotation Schedule
# ========================================
# Last rotation: <YYYY-MM-DD>
# Next rotation: <YYYY-MM-DD + 90 days>
# Rotated by: <name@organization.com>
